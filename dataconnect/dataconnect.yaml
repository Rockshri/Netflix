specVersion: "v1beta"
serviceId: "netflix-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "netflix-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
